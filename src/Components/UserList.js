// UserList.js
import React from 'react';

const UserList = ({ users, currentPage, usersPerPage, addToTeam }) => {
    return (
        <div className="user-container">
            {/* Your user list JSX */}
        </div>
    );
};

export default UserList;
