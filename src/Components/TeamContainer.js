// TeamContainer.js
import React from 'react';

const TeamContainer = ({ selectedTeamMembers }) => {
    return (
        <div className="team-container">
            {/* Your team details JSX */}
        </div>
    );
};

export default TeamContainer;
